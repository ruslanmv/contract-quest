# Allowed changes
Coder batches may edit only: `src/**`, `public/**`, `docs/**`.
Protected (no coder edits unless the batch explicitly allows): the `MATRIX_*` files,
`.github/workflows/**`, `package.json`, `tsconfig.json`, `vite.config.ts`.
